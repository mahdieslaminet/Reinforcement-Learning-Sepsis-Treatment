# 🧠 شبیه‌سازی تصمیم‌گیری بالینی با یادگیری تقویتی خارج از سیاست (Off-Policy RL)

این پروژه یک پیاده‌سازی از **یادگیری تقویتی خارج از سیاست (Off-Policy RL)** برای شبیه‌سازی تصمیم‌گیری بالینی ارائه می‌دهد. هدف اصلی این پروژه، آموزش یک عامل (Agent) برای پیش‌بینی تصمیمات درمانی به‌صورت بهینه بر اساس داده‌های مسیرهای بالینی (Trajectories) است.

این پروژه بر اساس مقاله‌ی

**Development of a Reinforcement Learning Algorithm to Optimize Corticosteroid Therapy in Critically Ill Patients with Sepsis**

الهام گرفته شده و پیاده‌سازی آن برای اهداف آموزشی و آزمایشی انجام شده است.

---

## 🔹 ویژگی‌ها

* ✅ **عامل A2C (Advantage Actor-Critic)**: مدل اصلی پروژه، یک عامل A2C است که با دریافت ویژگی‌های بالینی، اقدام مناسب (مانند تصمیم درمانی) را انتخاب می‌کند.
* 🧪 **تولید داده مصنوعی**: امکان تولید مجموعه داده‌های مصنوعی برای تست سریع و ارزیابی سیستم فراهم شده است.
* 📊 **ارزیابی خارج از سیاست (Off-Policy Evaluation)**: عملکرد عامل آموزش‌دیده با استفاده از مدل سیاست رفتار (Behavior Policy) ارزیابی می‌شود.
* 🔍 **تحلیل اهمیت ویژگی‌ها (Feature Importance)**: با استفاده از مدل Random Forest و روش Permutation Importance، بررسی می‌شود که کدام ویژگی‌ها بیشترین تأثیر را روی تصمیمات عامل دارند.
* 🌐 **رابط کاربری گریدیو (Gradio UI)**: امکان آموزش و ارزیابی مدل از طریق رابط وب، بدون نیاز به کدنویسی مستقیم.

---

## 📂 ساختار پروژه

```
├── agent.py              # پیاده‌سازی عامل A2C
├── data_loader.py        # بارگذاری و پردازش مسیرهای بالینی و تولید داده مصنوعی
├── eval_offpolicy.py     # ارزیابی خارج از سیاست با روش Weighted Importance Sampling
├── feature_importance.py # تحلیل اهمیت ویژگی‌ها
├── models.py             # معماری شبکه عصبی Actor-Critic
├── train.py              # اسکریپت اصلی آموزش عامل
├── ui.py                 # رابط کاربری با Gradio
├── requirements.txt      # پیش‌نیازها
└── synthetic_clinical_trajectories.npz  # مجموعه داده مصنوعی
```

---

## ⚙️ نصب و راه‌اندازی

برای شروع، مخزن را کلون کنید و پیش‌نیازها را نصب نمایید:

```bash
git clone https://github.com/yourusername/yourrepository.git
cd yourrepository
pip install -r requirements.txt
```

---

## 🏋️‍♂️ آموزش مدل

برای آموزش عامل، از دستور زیر استفاده کنید:

```bash
python train.py --data /path/to/dataset.npz --epochs 100 --lr 3e-4 --gamma 0.99
```

* `--data`: مسیر فایل مجموعه داده (فرمت `.npz`)
* `--epochs`: تعداد دوره‌های آموزش
* `--lr`: نرخ یادگیری
* `--gamma`: فاکتور تخفیف

> ⚠️ در صورت عدم ارائه مجموعه داده، از داده مصنوعی پیش‌فرض استفاده می‌شود.

---

## 📊 ارزیابی خارج از سیاست

پس از آموزش، می‌توانید عملکرد عامل را با استفاده از مدل سیاست رفتار (مثل تصمیمات واقعی پزشکان) ارزیابی کنید:

```bash
python eval_offpolicy.py --data /path/to/dataset.npz
```

خروجی شامل **WIS Estimate** و **حد پایین ۹۵٪** خواهد بود.

---

## 🔍 تحلیل اهمیت ویژگی‌ها

برای بررسی تأثیر هر ویژگی روی تصمیمات عامل:

```bash
python feature_importance.py --data /path/to/dataset.npz
```

---

## 🌐 رابط کاربری با Gradio

رابط کاربری گریدیو امکان آموزش و ارزیابی مدل را بدون نیاز به استفاده از دستورات فراهم می‌کند:

```bash
python ui.py
```

ویژگی‌ها:

* 📁 آپلود مجموعه داده
* ⚙️ تنظیم پارامترهای آموزش (تعداد دوره، نرخ یادگیری، گاما و …)
* 📈 نمایش نمودارهای آموزش و نتایج ارزیابی

---

## 🙏 قدردانی

این پروژه بر اساس مقاله‌ی

**Development of a Reinforcement Learning Algorithm to Optimize Corticosteroid Therapy in Critically Ill Patients with Sepsis**

توسعه یافته است و پیاده‌سازی آن صرفاً برای اهداف آموزشی و آزمایشی صورت گرفته است.

---
